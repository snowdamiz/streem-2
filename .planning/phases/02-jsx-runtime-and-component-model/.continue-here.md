---
phase: 02-jsx-runtime-and-component-model
task: post-execution-debugging
status: blocked
last_updated: 2026-02-28T06:37:10.177Z
---

<current_state>
Phase 02 executed successfully (5/5 plans complete, 93 tests passing). Verification ran and found one TypeScript gap (fixed). Now debugging a live browser runtime error in the demo app (`apps/demo`). The `<For>` component crashes with `TypeError: e.children is not a function` in the browser, even though unit tests pass.

The fix to `h.ts` (preserving `props.children` for the automatic JSX runtime) is in the dist and confirmed correct by integration tests. Debug logs have been added to `For` and the dist rebuilt. Waiting to see what the browser console outputs.
</current_state>

<completed_work>

- Plan 02-01: @streem/dom package scaffold, h(), jsx-runtime, render() — DONE
- Plan 02-02: Reactive DOM bindings (bindTextNode, bindAttr, etc.) — DONE
- Plan 02-03: Show, For, onMount primitives — DONE
- Plan 02-04: ErrorBoundary and Suspense — DONE
- Plan 02-05: HMR registry, Vite plugin, demo app — DONE
- Fix 1: TS2322 in For reconciler (null filter on result) — committed c9fc041
- Fix 2: `key` prop renamed to `by` — JSX reserves `key` as third arg to jsx() — committed 1cc0bb6
- Fix 3: h() now preserves `props.children` for automatic JSX runtime — committed 7fbd246
- Integration tests added to scaffold.test.ts proving h(For, { children: fn }) works — committed 83aa5e7
- Debug logs added to For component (compiled into dist) — NOT YET COMMITTED
</completed_work>

<remaining_work>

- **Active bug**: `TypeError: e.children is not a function` at `index.js:93` in browser
- Get the `[For] props keys:` and `[For] createRoot:` console output from the user
- Remove debug logs once root cause is found
- Fix whatever the root cause is
- Re-run verification (status was `human_needed` — live HMR test pending)
- Complete phase: update roadmap, run `phase complete`
</remaining_work>

<decisions_made>

- Renamed `For` prop `key` → `by` because JSX's `key` is extracted as the third arg to `jsx(type, props, key)` by TypeScript/esbuild and never reaches component props
- Fixed `h()` to NOT overwrite `props.children` when rest args are empty (automatic JSX runtime always puts children in props, not rest args)
- Used `pnpm --filter @streem/dom build` to rebuild dist after every fix (dist is what the dev server serves)
- Verification status was `human_needed` (all 10 must-haves passed, only live HMR test remains)
</decisions_made>

<blockers>

- **Browser serves stale `index.js`**: Despite rebuilding `@streem/dom` dist, the Vite dev server appears to serve a cached `index.js`. The crash is still at line 93, but the latest rebuild shifts it to line 95-96. User should try `pnpm --filter @streem/demo exec vite --force` to clear Vite's module cache + hard refresh (`Cmd+Shift+R`).
- **Root cause unknown**: The `[For] props keys:` console.log (added in latest rebuild) should reveal what `props.children` actually is at runtime. Without this output we're guessing.
</blockers>

<context>
The core h() logic for automatic JSX runtime is fixed and integration-tested. The working theory is that Vite's dev server caches `packages/dom/dist/index.js` in memory and doesn't notice when we rebuild the package (it only watches source files, not symlinked workspace package dist folders). The `--force` flag forces Vite to clear its dependency cache and re-read all files.

If the debug logs show `props.children type: function`, the issue is something else in the reconciliation loop (unlikely given tests pass). If they show `type: undefined` or `type: object`, then h() is still not passing children correctly — which would mean the browser is running old h.js despite the hash change.

Files changed this session (not yet committed):
- `packages/dom/src/components.ts` — debug console.log in For (should be removed after debugging)
- `packages/dom/dist/index.js` — rebuilt with debug logs
</context>

<next_action>
1. Ask user: "Run `pnpm --filter @streem/demo exec vite --force`, hard refresh (`Cmd+Shift+R`), and paste the `[For] props keys:` line from the browser console."
2. Once you have the output, diagnose:
   - If `children type: undefined` → h() fix isn't reaching the browser, investigate why
   - If `children type: object` (HTMLCollection) → something is setting `children` to a DOM property
   - If `children type: function` + still crashes → bug is inside the effect/createRoot loop
3. Remove debug logs from `components.ts` after fix is found
4. Rebuild dist, verify all 93 tests pass
5. Ask user to confirm HMR works (the remaining `human_needed` verification item)
6. Run `node /Users/sn0w/.claude/get-shit-done/bin/gsd-tools.cjs phase complete "02"` to finalize
</next_action>
