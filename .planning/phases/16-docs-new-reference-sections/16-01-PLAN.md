---
phase: 16-docs-new-reference-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/landing/src/DocsApp.tsx
autonomous: true
requirements:
  - DOCS-06

must_haves:
  truths:
    - "A 'Styling' nav item appears in the docs sidebar/nav and is clickable"
    - "Clicking 'Styling' renders a full StylingSection covering CSS Modules, Tailwind v4, and style objects"
    - "The Getting Started section contains a cross-link pointing to the Styling guide"
    - "All code examples in StylingSection are complete and runnable (no pseudocode)"
  artifacts:
    - path: "apps/landing/src/DocsApp.tsx"
      provides: "StylingSection function + nav entry + Show block + cross-link"
      contains: "StylingSection"
  key_links:
    - from: "NAV_ITEMS array"
      to: "StylingSection()"
      via: "Show when currentPage.value === 'styling'"
      pattern: "currentPage\\.value === 'styling'"
    - from: "GettingStartedSection"
      to: "styling nav item"
      via: "inline cross-link text referencing #styling"
      pattern: "#styling"
---

<objective>
Add the Styling reference section to the docs — a new nav item and section function covering CSS Modules setup, Tailwind v4 setup, reactive style objects, and what NOT to use. Incorporates all content from docs/STYLING.md and adds a Tailwind v4 subsection that STYLING.md lacks.

Purpose: DOCS-06 — Styling guide gives developers a single authoritative reference for styling decisions in Streem apps, preventing cargo-culted CSS-in-JS patterns.
Output: Updated DocsApp.tsx with StylingSection() function, 'styling' NAV_ITEM, Show block, and a cross-link from GettingStartedSection.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/landing/src/DocsApp.tsx
@docs/STYLING.md

<interfaces>
<!-- Key patterns from DocsApp.tsx that executor must follow exactly. -->

NAV_ITEMS (append after 'patterns'):
```typescript
{ id: 'styling', label: 'Styling' },
```

Section function signature (all return Node):
```typescript
function StylingSection(): Node {
  return (
    <DocSection id="styling" title="Styling">
      <p>...</p>
      <Code>{`...`}</Code>
      <h3 class="doc-section-subtitle">...</h3>
      ...
    </DocSection>
  ) as unknown as Node
}
```

Show block pattern (append after the patterns Show block):
```tsx
<Show when={() => currentPage.value === 'styling'}>
  {() => StylingSection()}
</Show>
```

Cross-link pattern used in existing sections (inline text with anchor):
```tsx
<p>See the <a href="#styling" class="docs-link">Styling guide</a> for CSS Modules and Tailwind v4 setup.</p>
```

CSS class for anchor links (add to inline style block):
```css
.docs-link {
  color: var(--color-primary, #3b82f6);
  text-decoration: none;
}
.docs-link:hover {
  text-decoration: underline;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StylingSection function with full content</name>
  <files>apps/landing/src/DocsApp.tsx</files>
  <action>
Add a `StylingSection()` function to DocsApp.tsx, positioned after `PatternsSection` and before `DocsApp`. The section must have three h3 sub-sections using `doc-section-subtitle` class:

1. **CSS Modules** — incorporate verbatim content from docs/STYLING.md: "No configuration needed — Vite handles .module.css files automatically." Include the Button.module.css + Button.tsx Code examples. Add the vite-env.d.ts TypeScript autocomplete tip.

2. **Tailwind v4** — this subsection is NOT in STYLING.md, add it fresh:
   - Install: `npm install -D tailwindcss @tailwindcss/vite`
   - vite.config.ts: `import tailwindcss from '@tailwindcss/vite'` then add `tailwindcss()` to plugins array
   - CSS entry file: `@import "tailwindcss";`
   - Example usage in TSX: a component using Tailwind utility classes on a div
   - TypeScript note: Tailwind classes are plain strings — no special types needed

3. **Reactive style objects** — incorporate content from docs/STYLING.md "Style objects" section: import `CSSProperties` from `streem`, show static object usage (`<div style={containerStyle} />`), show reactive accessor usage (`<div style={() => ({ background: isDark() ? '#1a1a1a' : 'white' })} />`).

Also add a fourth closing subsection **What NOT to use** — CSS-in-JS runtimes (styled-components, emotion) add runtime overhead; CSS Modules + style objects are sufficient.

Add `.docs-link` CSS rule to the inline `<style>` block inside DocsApp component:
```css
.docs-link { color: var(--color-primary, #3b82f6); text-decoration: none; }
.docs-link:hover { text-decoration: underline; }
```

Note on Signal API: In this codebase, signals use `.value` for reads inside effects (e.g. `currentPage.value`) and are called as functions in JSX expressions (e.g. `{count}` or `{() => count()}`). Follow the exact same patterns as existing sections — no `.get()`, no `.subscribe()`.
  </action>
  <verify>
    <automated>pnpm --filter @streem/landing exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>StylingSection function exists in DocsApp.tsx with three complete Code examples covering CSS Modules, Tailwind v4, and style objects. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire nav item, Show block, and cross-link</name>
  <files>apps/landing/src/DocsApp.tsx</files>
  <action>
Three wiring changes to DocsApp.tsx:

1. **NAV_ITEMS**: Append `{ id: 'styling', label: 'Styling' }` after the `patterns` entry (7th item).

2. **Show block**: Append inside the `docs-content` div, after the patterns Show block:
   ```tsx
   <Show when={() => currentPage.value === 'styling'}>
     {() => StylingSection()}
   </Show>
   ```

3. **Cross-link from GettingStartedSection**: In `GettingStartedSection()`, after the tsconfig/vite.config code example and before the Counter example paragraph, add:
   ```tsx
   <p>For styling setup (CSS Modules or Tailwind v4), see the <a href="#styling" class="docs-link">Styling guide</a>.</p>
   ```

Verify the hash routing still works: `getPage()` checks `NAV_ITEMS.some(i => i.id === hash)` — adding the new entry to NAV_ITEMS is sufficient for #styling to be a valid route.
  </action>
  <verify>
    <automated>pnpm --filter @streem/landing exec tsc --noEmit 2>&1 | tail -5 && grep -c "styling" /Users/sn0w/Documents/dev/streem-2/apps/landing/src/DocsApp.tsx</automated>
  </verify>
  <done>'styling' appears multiple times in DocsApp.tsx (NAV_ITEM, Show condition, StylingSection id, cross-link href). TypeScript passes. Navigating to #styling in browser renders the section.</done>
</task>

</tasks>

<verification>
Run TypeScript check: `pnpm --filter @streem/landing exec tsc --noEmit`
Grep confirms all four wiring points:
- `grep "id: 'styling'" apps/landing/src/DocsApp.tsx` — NAV_ITEM present
- `grep "StylingSection" apps/landing/src/DocsApp.tsx` — function defined and called
- `grep "#styling" apps/landing/src/DocsApp.tsx` — cross-link present
- `grep "currentPage.value === 'styling'" apps/landing/src/DocsApp.tsx` — Show block present
</verification>

<success_criteria>
1. StylingSection() function exists with CSS Modules, Tailwind v4, and reactive style object subsections
2. 'Styling' nav item is the 7th entry in NAV_ITEMS
3. Show block for 'styling' is present in DocsApp JSX
4. Cross-link from GettingStartedSection to #styling exists
5. TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-docs-new-reference-sections/16-01-SUMMARY.md`
</output>
