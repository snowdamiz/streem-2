---
phase: 17-make-the-landing-docs-page-produciton-ready-favicon-seo-etc-then-push-changes-to-release-to-deploy-changes
plan: "03"
type: execute
wave: 2
depends_on:
  - "17-01"
  - "17-02"
files_modified: []
autonomous: false
requirements:
  - PROD-04

must_haves:
  truths:
    - "The Vite build compiles successfully with no TypeScript errors after the HTML changes"
    - "The built dist/ folder contains favicon.svg copied from public/"
    - "Pushing these changes to main triggers the GitHub Actions deploy workflow"
  artifacts:
    - path: "apps/landing/dist/favicon.svg"
      provides: "Favicon in build output, confirming Vite correctly copies public/ assets"
      min_lines: 1
  key_links:
    - from: "apps/landing/public/favicon.svg"
      to: "apps/landing/dist/favicon.svg"
      via: "Vite build copies public/ to dist/"
      pattern: "favicon\\.svg"
---

<objective>
Verify the build works end-to-end and confirm the deployment is triggered by pushing to main.

Purpose: Plans 01 and 02 added new assets and HTML changes. This plan verifies the full Vite build succeeds, confirms favicon.svg is in the build output, and documents that pushing to main triggers the GitHub Actions workflow that deploys to GitHub Pages. No code changes needed — this is verification + deploy trigger.
Output: Confirmed working build output; changes pushed to main which triggers CI deployment.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Deployment mechanism (from .github/workflows/deploy-landing.yml):
- Trigger: push to main when apps/landing/** or packages/** change
- Build command: pnpm --filter /landing build
- Output dir: apps/landing/dist
- Deploy target: GitHub Pages

The "release" mentioned in the phase name refers to the GitHub Actions deployment triggered by
pushing to main. There is no separate release branch — main IS the release branch.

Vite copies everything from apps/landing/public/ into the dist/ root automatically, so
favicon.svg placed in public/ will appear at dist/favicon.svg after build.

The TypeScript check (`tsc --noEmit`) runs as part of the build script in package.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build to verify no TypeScript errors and favicon is copied</name>
  <files></files>
  <action>
    Run the full production build for the landing app and verify the output:

    1. Run the build:
       ```
       cd /Users/sn0w/Documents/dev/streem-2 && pnpm --filter /landing build
       ```

    2. Verify favicon.svg was copied to dist:
       ```
       ls apps/landing/dist/favicon.svg
       ```

    3. Verify the dist/index.html (root) contains og:title meta tag (confirming the OG tags were included):
       ```
       grep "og:title" apps/landing/dist/index.html
       ```

    4. Verify the dist/docs/index.html contains og:title:
       ```
       grep "og:title" apps/landing/dist/docs/index.html
       ```

    If the build fails with TypeScript errors, fix the errors before proceeding to the checkpoint.
    If favicon.svg is missing from dist/, check that it was placed in public/ (not src/).

    Do NOT commit or push in this task — that happens after the checkpoint.
  </action>
  <verify>
    <automated>ls /Users/sn0w/Documents/dev/streem-2/apps/landing/dist/favicon.svg && grep "og:title" /Users/sn0w/Documents/dev/streem-2/apps/landing/dist/index.html && grep "og:title" /Users/sn0w/Documents/dev/streem-2/apps/landing/dist/docs/index.html</automated>
  </verify>
  <done>
    Build succeeds (exit 0), dist/favicon.svg exists, both dist/index.html and dist/docs/index.html contain og:title.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — favicon and OG preview look correct</name>
  <files>apps/landing/public/favicon.svg</files>
  <action>
    Human verifies the favicon appears correctly in the browser tab and OG meta tags are correct.
    Run `pnpm preview` in apps/landing and open http://localhost:4173/streem-2/ to check.
  </action>
  <what-built>
    1. favicon.svg created from Streem logo mark icon (two chevron shapes on dark background)
    2. OG + Twitter card meta tags added to landing page index.html
    3. OG + Twitter card meta tags + description added to docs/index.html
    4. Full production build verified — no TypeScript errors, favicon in dist/
  </what-built>
  <how-to-verify>
    1. Preview the build locally:
       cd /Users/sn0w/Documents/dev/streem-2/apps/landing && pnpm preview
       Then visit http://localhost:4173/streem-2/ and http://localhost:4173/streem-2/docs/

    2. Check the favicon appears in the browser tab for both pages.

    3. Optionally test the OG tags with a social card preview tool:
       - Open https://www.opengraph.xyz/ or https://metatags.io/
       - Enter the GitHub Pages URL (after deployment): https://{owner}.github.io/streem-2/

    4. If everything looks good, type "approved" to proceed.
       If the favicon looks wrong (wrong shape, wrong size, not visible) or OG tags have
       incorrect URLs, describe the issue and the executor will fix it.
  </how-to-verify>
  <verify>
    <automated>echo "Human verification required — check browser tab for favicon"</automated>
  </verify>
  <done>User approves favicon appearance and OG tag content.</done>
  <resume-signal>Type "approved" to confirm and trigger the deployment push, or describe issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Stage and commit all production-ready changes to trigger deployment</name>
  <files></files>
  <action>
    Stage and commit all changed files to main. The GitHub Actions workflow
    (.github/workflows/deploy-landing.yml) automatically deploys to GitHub Pages on push to main
    when apps/landing/** files change — so committing to main IS the deploy trigger.

    Files to stage:
    - apps/landing/public/favicon.svg (new)
    - apps/landing/index.html (modified — OG + Twitter tags)
    - apps/landing/docs/index.html (modified — description + OG + Twitter tags)

    Commit message:
      feat(17): add favicon and SEO meta tags to landing and docs pages

    Then push to main:
      git push origin main

    After push, verify the GitHub Actions workflow is queued:
      gh workflow list --repo {owner}/streem-2 (if gh CLI is authenticated)
      Or check: https://github.com/{owner}/streem-2/actions

    The deployment will complete in ~2-3 minutes. The deployed site will be at:
      https://{owner}.github.io/streem-2/
  </action>
  <verify>
    <automated>git -C /Users/sn0w/Documents/dev/streem-2 log --oneline -1</automated>
  </verify>
  <done>
    Commit exists on main with favicon.svg and OG meta changes. Push to origin main was successful (no rejected error). GitHub Actions workflow triggered.
  </done>
</task>

</tasks>

<verification>
1. `ls apps/landing/dist/favicon.svg` — favicon in build output
2. `grep "og:title" apps/landing/dist/index.html` — OG tags in built landing page
3. `git log --oneline -3` — shows the production-ready commit
4. `git status` — working tree clean after push
</verification>

<success_criteria>
- Full Vite build passes with no errors
- favicon.svg is in dist/ after build
- Both HTML files have correct OG and Twitter card meta tags in the build output
- Changes committed and pushed to main
- GitHub Actions deployment workflow triggered
</success_criteria>

<output>
After completion, create `.planning/phases/17-make-the-landing-docs-page-produciton-ready-favicon-seo-etc-then-push-changes-to-release-to-deploy-changes/17-03-SUMMARY.md`
</output>
