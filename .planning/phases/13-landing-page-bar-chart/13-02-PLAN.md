---
phase: 13-landing-page-bar-chart
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified: []
autonomous: false
requirements:
  - LAND-01

must_haves:
  truths:
    - "Bar chart is visible on the landing page (localhost dev server)"
    - "Three benchmark suites (signal, computed, effect) are visible as grouped bar clusters"
    - "Three libraries (Streem, Preact, SolidJS) are represented per cluster with distinct colors"
    - "On a 375px-wide viewport the chart scales without horizontal scrollbar"
    - "Hovering over a bar dims the non-hovered bars (Streem signals driving opacity reactivity)"
  artifacts:
    - path: "apps/landing/src/components/BenchmarkChart.tsx"
      provides: "Verified chart component"
      min_lines: 80
  key_links:
    - from: "User's browser"
      to: "BenchmarkChart section"
      via: "scroll on landing page"
      pattern: "visible section with bar chart"
---

<objective>
Human visual verification that the BenchmarkChart renders correctly on the landing page — desktop and mobile widths, correct data, and reactive hover state working.

Purpose: This is a UI component that requires human eyes to verify layout, responsive behavior, and visual correctness. Automated tests cannot confirm the chart "looks right."

Output: No file changes — this is a verification checkpoint only.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-landing-page-bar-chart/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the landing dev server</name>
  <files></files>
  <action>
Start the landing app dev server so the user can inspect the chart:

```bash
cd /Users/sn0w/Documents/dev/streem-2
pnpm --filter /landing dev
```

The server will print the local URL (typically http://localhost:5173 or http://localhost:5174).
Print that URL clearly so the user knows where to go.
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/streem-2 && pnpm --filter /landing build 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server is running and accessible at localhost URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of bar chart on landing page</name>
  <action>Pause for user to inspect the BenchmarkChart in the browser and confirm visual correctness, responsive behavior, and hover reactivity.</action>
  <what-built>
    BenchmarkChart TSX component (Plan 13-01) — a Streem-dogfooded SVG grouped bar chart showing signal/computed/effect ops/sec for Streem, Preact, and SolidJS. Wired into App.tsx between the TickerDemo and CodeSample sections.
  </what-built>
  <how-to-verify>
    1. Open the landing page in your browser (URL printed by the dev server above)
    2. Scroll down past the "Real-time data, zero overhead" section (ticker table)
    3. You should see a "Performance / Benchmark comparison" section with a grouped SVG bar chart
    4. Verify the chart shows THREE clusters labelled: signal, computed, effect
    5. Verify each cluster has THREE bars (Streem white, Preact ~indigo, SolidJS ~blue)
    6. Verify bar heights look proportionally correct for signal: Streem ≈ Preact >> SolidJS
    7. Verify ops/sec labels appear above bars (e.g. "46.8M" over Streem signal bar)
    8. Verify a legend row shows colored dots labelled Streem / Preact / SolidJS
    9. Verify the footnote mentions "Primitive only · tinybench · Apple M4"
    10. HOVER TEST: Hover over any bar — the other bars should dim (opacity change). This proves Streem signals are driving the reactivity.
    11. MOBILE: Resize browser to 375px width. The chart should scale down without a horizontal scrollbar. Content should remain readable (not cut off).
  </how-to-verify>
  <resume-signal>Type "approved" if the chart looks correct, or describe any visual issues to fix.</resume-signal>
  <verify>
    <automated>echo "CHECKPOINT — human verification required"</automated>
  </verify>
  <done>User types "approved" confirming chart is visually correct on desktop and mobile</done>
</task>

</tasks>

<verification>
Human verified via browser inspection that:
- Chart section is present and positioned correctly on the landing page
- Three suites x three libraries rendered as grouped bars
- Hover opacity animation works (confirming Streem signals drive reactivity)
- Responsive at 375px viewport width
</verification>

<success_criteria>
User types "approved" confirming:
1. Bar chart visible and positioned on landing page
2. Three benchmark groups with three bars each are shown
3. Hover interaction dims non-hovered bars
4. Chart renders without overflow at 375px mobile width
5. Data labels above bars match BENCHMARKS.md ops/sec figures
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-page-bar-chart/13-02-SUMMARY.md`
</output>
