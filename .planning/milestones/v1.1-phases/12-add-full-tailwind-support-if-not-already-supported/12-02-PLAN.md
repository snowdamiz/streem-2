---
phase: 12-add-full-tailwind-support-if-not-already-supported
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-streem/templates/default/package.json
  - packages/create-streem/templates/default/vite.config.ts
  - packages/create-streem/templates/default/index.html
  - packages/create-streem/templates/default/src/App.tsx
  - packages/create-streem/templates/default/src/main.tsx
  - packages/create-streem/templates/default/src/vite-env.d.ts
  - packages/create-streem/templates/default/.vscode/extensions.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "A freshly scaffolded project from create-streem includes Tailwind v4 pre-configured — no manual setup needed"
    - "Running pnpm dev in a scaffolded project starts with Tailwind utility classes visible in the App.tsx counter UI"
    - "VS Code Tailwind IntelliSense is recommended via .vscode/extensions.json in scaffolded projects"
    - "App.tsx demo looks visually styled on first boot — not a blank unstyled page"
    - "The template build passes TypeScript and Vite checks with Tailwind v4 configured"
  artifacts:
    - path: "packages/create-streem/templates/default/package.json"
      provides: "@tailwindcss/vite and tailwindcss devDependencies in template"
      contains: "@tailwindcss/vite"
    - path: "packages/create-streem/templates/default/vite.config.ts"
      provides: "tailwind() Vite plugin in template"
      contains: "tailwind"
    - path: "packages/create-streem/templates/default/src/App.tsx"
      provides: "Visually styled counter demo using Tailwind classes"
      contains: "bg-white"
    - path: "packages/create-streem/templates/default/.vscode/extensions.json"
      provides: "VS Code Tailwind IntelliSense recommendation"
      contains: "bradlc.vscode-tailwindcss"
  key_links:
    - from: "packages/create-streem/templates/default/src/main.tsx"
      to: "./styles.css"
      via: "import statement"
      pattern: "import.*styles\\.css"
    - from: "packages/create-streem/templates/default/src/styles.css"
      to: "Tailwind CSS engine"
      via: "@import directive"
      pattern: "@import .tailwindcss."
---

<objective>
Bake Tailwind CSS v4 into the `create-streem` default template so that every project scaffolded with `npm create streem@latest` starts with Tailwind pre-configured. Update template package.json deps, vite.config.ts plugin, create a CSS entry with `@import "tailwindcss"`, style the App.tsx counter demo with Tailwind classes, and add `.vscode/extensions.json` recommending Tailwind IntelliSense.

Purpose: After the landing page audit (Plan 01) proves Tailwind v4 works with Streem JSX, the template update delivers this capability to all new projects without any manual configuration.

Output: Updated create-streem default template with Tailwind v4 pre-wired and a styled demo App.tsx.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-add-full-tailwind-support-if-not-already-supported/12-CONTEXT.md

<interfaces>
<!-- Current template files the executor will modify. -->

From packages/create-streem/templates/default/package.json (current):
```json
{
  "name": "streem-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "streem": "latest"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "typescript": "~5.8.0"
  }
}
```

From packages/create-streem/templates/default/vite.config.ts (current):
```typescript
import { defineConfig } from 'vite'
import { streemHMR } from 'streem'

export default defineConfig({
  plugins: [streemHMR()],
})
```

From packages/create-streem/templates/default/src/App.tsx (current):
```typescript
import { signal } from 'streem'

const count = signal(0)

export function App() {
  return (
    <div>
      <h1>Streem Counter</h1>
      <p>Count: {() => count()}</p>
      <button onClick={() => count.set(count() + 1)}>+</button>
      <button onClick={() => count.set(count() - 1)}>-</button>
    </div>
  )
}
```

From packages/create-streem/templates/default/src/main.tsx (current):
```typescript
import { render } from 'streem'
import { App } from './App'

render(App, document.getElementById('app')!)
```

From packages/create-streem/templates/default/tsconfig.json:
```json
{
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "streem",
    "types": ["vite/client"]
  }
}
```

From packages/create-streem/src/index.ts — CLI flow:
- Copies templates/default/ to target directory
- Patches package.json name with user input
- Runs package manager install
- No selection prompt — default template is the only template
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update template package.json and vite.config.ts with Tailwind v4 dependencies</name>
  <files>
    packages/create-streem/templates/default/package.json,
    packages/create-streem/templates/default/vite.config.ts
  </files>
  <action>
    1. Update packages/create-streem/templates/default/package.json to add Tailwind v4 devDependencies:
       ```json
       {
         "name": "streem-app",
         "private": true,
         "version": "0.0.0",
         "type": "module",
         "scripts": {
           "dev": "vite",
           "build": "vite build",
           "preview": "vite preview"
         },
         "dependencies": {
           "streem": "latest"
         },
         "devDependencies": {
           "@tailwindcss/vite": "latest",
           "tailwindcss": "latest",
           "vite": "^7.0.0",
           "typescript": "~5.8.0"
         }
       }
       ```
       No postcss.config.js or tailwind.config.js — Tailwind v4 is entirely plugin-driven.

    2. Update packages/create-streem/templates/default/vite.config.ts to import and register the Tailwind plugin. tailwind() MUST be listed before streemHMR() so Tailwind processes CSS before HMR transforms:
       ```typescript
       import { defineConfig } from 'vite'
       import tailwind from '@tailwindcss/vite'
       import { streemHMR } from 'streem'

       export default defineConfig({
         plugins: [tailwind(), streemHMR()],
       })
       ```
  </action>
  <verify>
    <automated>grep "@tailwindcss/vite" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/package.json && grep "tailwind" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/vite.config.ts</automated>
  </verify>
  <done>@tailwindcss/vite and tailwindcss in devDependencies. tailwind() plugin registered before streemHMR() in vite.config.ts. No postcss.config.js or tailwind.config.js created.</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS entry, style App.tsx counter demo, add .vscode/extensions.json</name>
  <files>
    packages/create-streem/templates/default/src/main.tsx,
    packages/create-streem/templates/default/src/styles.css,
    packages/create-streem/templates/default/src/App.tsx,
    packages/create-streem/templates/default/.vscode/extensions.json
  </files>
  <action>
    1. Create packages/create-streem/templates/default/src/styles.css as the CSS entry point:
       ```css
       @import "tailwindcss";
       ```
       Tailwind v4 uses a single @import — no separate base/components/utilities directives needed.

    2. Update packages/create-streem/templates/default/src/main.tsx to import styles.css before App:
       ```typescript
       import './styles.css'
       import { render } from 'streem'
       import { App } from './App'

       render(App, document.getElementById('app')!)
       ```

    3. Update packages/create-streem/templates/default/src/App.tsx to style the counter demo with Tailwind classes. The demo should look visually complete and "alive" on first boot — proving Tailwind works:
       ```typescript
       import { signal } from 'streem'

       const count = signal(0)

       export function App() {
         return (
           <div class="min-h-screen bg-gray-950 text-white flex items-center justify-center">
             <div class="bg-gray-900 rounded-2xl p-10 shadow-xl text-center w-80">
               <h1 class="text-2xl font-bold mb-2 tracking-tight">Streem Counter</h1>
               <p class="text-gray-400 text-sm mb-8">Fine-grained reactive signal</p>
               <div class="text-6xl font-mono font-bold mb-8 text-violet-400">
                 {() => count()}
               </div>
               <div class="flex gap-3 justify-center">
                 <button
                   class="flex-1 py-2 px-4 rounded-lg bg-gray-800 hover:bg-gray-700 text-lg font-bold transition-colors"
                   onClick={() => count.set(count() - 1)}
                 >
                   −
                 </button>
                 <button
                   class="flex-1 py-2 px-4 rounded-lg bg-violet-600 hover:bg-violet-500 text-white text-lg font-bold transition-colors"
                   onClick={() => count.set(count() + 1)}
                 >
                   +
                 </button>
               </div>
             </div>
           </div>
         )
       }
       ```
       All Tailwind classes are static string literals — scannable at build time, no purging gaps.

    4. Create packages/create-streem/templates/default/.vscode/extensions.json recommending Tailwind IntelliSense:
       ```json
       {
         "recommendations": [
           "bradlc.vscode-tailwindcss"
         ]
       }
       ```
       Tailwind IntelliSense v4 picks up `class` and `className` attributes in .tsx files without additional configuration. If any extra config were needed (e.g., customData), it would be documented here as a comment, but v4 CSS-based config is detected automatically by the extension.

    5. Verify the template builds cleanly by running a type-check. Since the template uses `streem` as `latest` (not workspace:*), we cannot run a full vite build in the monorepo, but we can verify the template files are structurally correct by checking that all class attributes use `class` (not `className` which works in React but is the Streem convention). Note: in Streem JSX, `class` and `className` are both accepted (per Phase 11 decision).
  </action>
  <verify>
    <automated>ls /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/styles.css && grep "@import" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/styles.css && grep "bradlc.vscode-tailwindcss" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/.vscode/extensions.json && grep "bg-violet-600" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/App.tsx</automated>
  </verify>
  <done>
    - src/styles.css exists with @import "tailwindcss" as its only content
    - main.tsx imports styles.css before render()
    - App.tsx uses Tailwind classes for a visually styled dark-mode counter (bg-gray-950, text-violet-400, etc.)
    - .vscode/extensions.json recommends bradlc.vscode-tailwindcss
    - All Tailwind classes in App.tsx are static string literals
  </done>
</task>

</tasks>

<verification>
```bash
# Verify all template files exist and contain expected content
ls /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/styles.css
ls /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/.vscode/extensions.json

# Tailwind in package.json
grep "@tailwindcss" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/package.json

# Plugin in vite.config.ts
grep "tailwind" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/vite.config.ts

# CSS @import
grep "@import" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/styles.css

# Tailwind classes in App.tsx
grep "bg-gray-950\|text-violet-400\|rounded-2xl" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/App.tsx

# VS Code extension
grep "bradlc" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/.vscode/extensions.json

# styles.css imported in main.tsx
grep "styles.css" /Users/sn0w/Documents/dev/streem-2/packages/create-streem/templates/default/src/main.tsx
```
</verification>

<success_criteria>
- Template package.json includes @tailwindcss/vite and tailwindcss in devDependencies
- Template vite.config.ts registers tailwind() before streemHMR() in plugins array
- src/styles.css exists with `@import "tailwindcss"` as its content
- main.tsx imports styles.css before render()
- App.tsx counter demo uses Tailwind classes: dark background, violet accent, rounded cards, hover states
- .vscode/extensions.json recommends bradlc.vscode-tailwindcss
- No postcss.config.js or tailwind.config.js in the template
- All Tailwind classes in App.tsx are static string literals (no dynamic class name construction)
</success_criteria>

<output>
After completion, create `.planning/phases/12-add-full-tailwind-support-if-not-already-supported/12-02-SUMMARY.md`
</output>
