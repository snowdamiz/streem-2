---
phase: 09-performance-benchmarks
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - BENCHMARKS.md
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "BENCHMARKS.md exists in the repository root and contains ops/second numbers for all three libraries"
    - "BENCHMARKS.md documents the methodology: what was measured, Node version, hardware class, and instructions to reproduce"
    - "BENCHMARKS.md is committed to git and readable without running any code"
  artifacts:
    - path: "BENCHMARKS.md"
      provides: "Committed benchmark results with methodology and reproduction instructions"
      contains: "ops/sec"
      min_lines: 40
  key_links:
    - from: "BENCHMARKS.md"
      to: "apps/bench/run.mjs"
      via: "reproduction instructions section"
      pattern: "node apps/bench/run.mjs"
---

<objective>
Run the benchmark suite from Plan 01, capture the output, and commit BENCHMARKS.md to the repository root with full methodology documentation.

Purpose: PERF-02 requires committed results — not just a working script. The results must be verifiable by reading the file, with clear instructions to reproduce.
Output: BENCHMARKS.md committed to the repository root containing ops/second tables, environment metadata (Node version, hardware class), and reproduction steps.
</objective>

<execution_context>
@/Users/sn0w/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sn0w/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-benchmarks/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run benchmarks and capture output</name>
  <files>
    BENCHMARKS.md
  </files>
  <action>
Run the benchmark suite and capture the output for inclusion in BENCHMARKS.md.

Step 1: Ensure /core dist is fresh:
```
pnpm --filter /core build
```

Step 2: Run the benchmarks and capture output:
```
node /Users/sn0w/Documents/dev/streem-2/apps/bench/run.mjs 2>&1
```

Step 3: Write BENCHMARKS.md to the repository root. Structure:

```markdown
# /core Signal Benchmarks

Benchmark results for `/core` signals compared against `solid-js` and
`@preact/signals-core` under identical conditions using
[tinybench](https://github.com/tinylibs/tinybench).

## Methodology

**What was measured:**
- `signal()` / `createSignal` — single read+write cycle throughput (ops/second)
- `computed()` / `createMemo()` — derived value re-evaluation on signal change
- `effect()` / `createEffect()` — reactive side-effect re-run on signal change

Each benchmark runs 1000 warmup iterations followed by 5000 measured iterations.
All libraries run in the same process under the same Node.js version.
No DOM, no framework overhead — pure reactive primitive cost.

**Libraries compared:**
| Library | Version | Import |
|---------|---------|--------|
| /core | 0.1.0 | `signal, computed, effect, createRoot` |
| @preact/signals-core | 1.x | `signal, computed, effect` |
| solid-js | 1.x | `createSignal, createMemo, createEffect, createRoot` |

**Environment:**
- Node.js: [filled from run output]
- Hardware: [CPU model class — e.g., "Apple M-series" or "Intel x86_64"]
- Date: [date from run]
- OS: macOS / Linux / Windows
- Benchmark tool: tinybench ^2.9.0

## Results

[paste the full table output from `node apps/bench/run.mjs` here]

## How to Reproduce

```sh
# From the repository root:
pnpm install
pnpm --filter /core build
node apps/bench/run.mjs
```

Results vary by machine. The tables above were captured on the hardware described in Environment.

## Interpretation

- Higher ops/sec = faster
- These numbers measure primitive overhead only. Real app performance depends on
  component count, update frequency, and DOM work — not captured here.
- /core is a young library. These benchmarks establish a baseline for future
  optimization work.
```

Fill in the actual values from the benchmark run output:
- Replace `[filled from run output]` with the actual Node.js version (from `process.version` in run.mjs output)
- Replace `[CPU model class]` with the class of hardware (read from `sysctl -n machdep.cpu.brand_string` on macOS or `/proc/cpuinfo` on Linux — use the brand string)
- Replace `[date from run]` with the ISO date from the output header
- Replace `[paste the full table output...]` with the actual ops/second tables from stdout
- Fill in the actual library versions from `node_modules/@preact/signals-core/package.json` and `node_modules/solid-js/package.json` version fields

Detect hardware class:
```sh
sysctl -n machdep.cpu.brand_string 2>/dev/null || cat /proc/cpuinfo 2>/dev/null | grep "model name" | head -1 | cut -d: -f2 | xargs
```
  </action>
  <verify>
    <automated>grep -l "ops/sec" /Users/sn0w/Documents/dev/streem-2/BENCHMARKS.md && grep -l "node apps/bench/run.mjs" /Users/sn0w/Documents/dev/streem-2/BENCHMARKS.md && echo "BENCHMARKS.md valid"</automated>
  </verify>
  <done>
    BENCHMARKS.md exists in the repository root.
    File contains the string "ops/sec" (actual results present).
    File contains "node apps/bench/run.mjs" (reproduction instructions present).
    File contains Node.js version and hardware class in the Environment section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit benchmark results and add bench script to root package.json</name>
  <files>
    BENCHMARKS.md
    package.json
  </files>
  <action>
Step 1: Add a `bench` script to the root `package.json` so the benchmark suite is easy to discover:

Read the current root `package.json` (it has `build` and `test` scripts). Add:
```json
"bench": "node apps/bench/run.mjs"
```

So the scripts section becomes:
```json
"scripts": {
  "build": "pnpm -r build",
  "test": "vitest run",
  "test:watch": "vitest",
  "bench": "node apps/bench/run.mjs"
}
```

Step 2: Commit both files — BENCHMARKS.md and package.json — with a meaningful commit message:
```
git add BENCHMARKS.md apps/bench/ package.json
git commit -m "feat(bench): add signal benchmark suite and committed results (PERF-01, PERF-02)"
```

Verify the commit is in git log:
```
git log --oneline -3
```

Step 3: Confirm BENCHMARKS.md is tracked:
```
git show --stat HEAD
```
  </action>
  <verify>
    <automated>cd /Users/sn0w/Documents/dev/streem-2 && git log --oneline -1 | grep -i bench && git show --name-only HEAD | grep "BENCHMARKS.md" && echo "commit ok"</automated>
  </verify>
  <done>
    Root package.json has `"bench": "node apps/bench/run.mjs"` in scripts.
    BENCHMARKS.md is committed to git (appears in `git show --name-only HEAD`).
    apps/bench/ files are committed.
    `pnpm bench` from repository root runs the benchmark suite without errors.
  </done>
</task>

</tasks>

<verification>
Final check — from repository root:
```sh
# Verify BENCHMARKS.md is in git
git show HEAD:BENCHMARKS.md | head -5

# Verify bench script works
pnpm bench 2>&1 | tail -5
```
Both should succeed without errors.
</verification>

<success_criteria>
- BENCHMARKS.md exists at repository root, committed to git
- File contains ops/second tables for all 9 benchmark cases (3 primitives × 3 libraries)
- File documents: Node.js version, hardware class, date, reproduction command
- Root package.json has `"bench": "node apps/bench/run.mjs"` script
- `pnpm bench` runs end-to-end from repository root and exits 0
- Phase 9 requirements PERF-01 and PERF-02 are both satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-benchmarks/09-02-SUMMARY.md`
</output>
